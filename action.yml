name: 'Add Smartsheet Row'
description: 'Adds a new row to a Smartsheet'
inputs:
  access-token:
    description: 'Smartsheet API access token'
    required: true
  sheet-id:
    description: 'Smartsheet sheet ID'
    required: true
outputs:
  row-id:
    description: 'The created row's ID'
runs:
  using: 'node12'
  main: 'dist/index.js'
